Effective configuration
=======================

The fully merged Snakemake configuration used to build this DAG, including
command-line overrides. Project-internal paths are relative and external
absolute paths are redacted. Defaults, the active override and this effective
snapshot are hashed separately in the run manifest.
