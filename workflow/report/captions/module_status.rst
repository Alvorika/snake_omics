Module status
=============

The status table distinguishes completed modules, modules that were not
requested, QC that still needs review, flagged QC, and analysis branches that
completed without an eligible ROI or with per-ROI model failures. A failed
Snakemake rule prevents report generation and is therefore not mislabeled as a
completed report module.
