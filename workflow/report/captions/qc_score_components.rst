QC score evidence
=================

One row per sample and QC component. ``status`` separates measured evidence
from ``PENDING``, ``NA`` and ``UNCALIBRATED`` states; the latter states do not
silently contribute points. Manual image-alignment and spatial-artifact
decisions come from the review table recorded in the run configuration.
