# Schemas

`config.schema.yaml` and `samples.schema.yaml` are loaded when Snakemake parses the workflow. Invalid configuration therefore fails before any input inspection or expensive computation begins.
